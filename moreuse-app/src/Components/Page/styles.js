import styled from "styled-components";
import { TopbarHeigh } from "../../globalStyles";

export const PageContainer = styled.section`
  margin-top: ${TopbarHeigh + 20}px;
`;

export const PageTitleContainer = styled.div`
  margin: 0 20px;
  text-align: center;
`;

